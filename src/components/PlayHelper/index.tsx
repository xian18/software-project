import React,{FC,memo} from 'react';
import {useSnackbar} from 'notistack';

const PlayHelper:FC=memo(({})=>{

    const {enqueueSnackbar,closeSnackbar}=useSnackbar();

    return (
        <>
            
        </>
    )
})

export default PlayHelper;